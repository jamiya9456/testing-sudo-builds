sudo: required

language: php
php: 7.1
env:
  global:
    - HELLO="world"
    - PATH="$TRAVIS_BUILD_DIR:$PATH"

install:
  - ls
  - bash run.sh
  
before_script:
  - ls -al

script:
  - ls

after_script:
  - sudo ls

cache:
  directories:
    - $HOME/.composer/cache

notifications:
  email:
    on_success: never
    on_failure: change
  #- sudo php easyengine.phar cli info
